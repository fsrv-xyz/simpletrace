---
default:
  image: golang:latest@sha256:9820aca42262f58451f006de3213055974b36f24b31508c1baa73c967fcecb99

stages:
  - test

format:
  stage: test
  allow_failure: true
  script:
    - go fmt $(go list ./... | grep -v /vendor/)
    - go vet $(go list ./... | grep -v /vendor/)
    - go test -race $(go list ./... | grep -v /vendor/)

