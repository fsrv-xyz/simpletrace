---
default:
  image: golang:latest@sha256:769c7a5afa20298a84e3105a7b927610129b4c1ed90444e6043f64230beb088b

stages:
  - test

format:
  stage: test
  allow_failure: true
  script:
    - go fmt $(go list ./... | grep -v /vendor/)
    - go vet $(go list ./... | grep -v /vendor/)
    - go test -race $(go list ./... | grep -v /vendor/)

