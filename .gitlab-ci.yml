---
default:
  image: golang:latest@sha256:a0e3e6859220ee48340c5926794ce87a891a1abb51530573c694317bf8f72543

stages:
  - test

format:
  stage: test
  allow_failure: true
  script:
    - go fmt $(go list ./... | grep -v /vendor/)
    - go vet $(go list ./... | grep -v /vendor/)
    - go test -race $(go list ./... | grep -v /vendor/)

