---
default:
  image: golang:latest@sha256:6974950a29db63cc5712fc3859904b2090feef256d184a6a4e0224b541db016c

stages:
  - test

format:
  stage: test
  allow_failure: true
  script:
    - go fmt $(go list ./... | grep -v /vendor/)
    - go vet $(go list ./... | grep -v /vendor/)
    - go test -race $(go list ./... | grep -v /vendor/)

