---
default:
  image: golang:latest@sha256:3952625c7e85474d49f070f5cb8eaa8a7a10672870b63a004645110c6e33162b

stages:
  - test

format:
  stage: test
  allow_failure: true
  script:
    - go fmt $(go list ./... | grep -v /vendor/)
    - go vet $(go list ./... | grep -v /vendor/)
    - go test -race $(go list ./... | grep -v /vendor/)

