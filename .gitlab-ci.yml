---
default:
  image: golang:latest@sha256:18a1f2d1e1d3c49f27c904e9182375169615c65852ace724987929b910195b2c

stages:
  - test

format:
  stage: test
  allow_failure: true
  script:
    - go fmt $(go list ./... | grep -v /vendor/)
    - go vet $(go list ./... | grep -v /vendor/)
    - go test -race $(go list ./... | grep -v /vendor/)

