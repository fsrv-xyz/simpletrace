---
default:
  image: golang:latest@sha256:519184356f970849c5f58764014c13ce20c3a3cadc13be7b8d87269bc5554ccd

stages:
  - test

format:
  stage: test
  allow_failure: true
  script:
    - go fmt $(go list ./... | grep -v /vendor/)
    - go vet $(go list ./... | grep -v /vendor/)
    - go test -race $(go list ./... | grep -v /vendor/)

