---
default:
  image: golang:latest@sha256:ef61a20960397f4d44b0e729298bf02327ca94f1519239ddc6d91689615b1367

stages:
  - test

format:
  stage: test
  allow_failure: true
  script:
    - go fmt $(go list ./... | grep -v /vendor/)
    - go vet $(go list ./... | grep -v /vendor/)
    - go test -race $(go list ./... | grep -v /vendor/)

