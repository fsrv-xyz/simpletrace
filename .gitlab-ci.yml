---
default:
  image: golang:latest@sha256:efa59042e5f808134d279113530cf419e939d40dab6475584a13c62aa8497c64

stages:
  - test

format:
  stage: test
  allow_failure: true
  script:
    - go fmt $(go list ./... | grep -v /vendor/)
    - go vet $(go list ./... | grep -v /vendor/)
    - go test -race $(go list ./... | grep -v /vendor/)

